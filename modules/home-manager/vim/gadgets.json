{
  "adapters": {
    "multi-session": {
      "host": "${host}",
      "port": "${port}"
    },
    "netcoredbg": {
      "attach": {
        "pidProperty": "processId",
        "pidSelect": "ask"
      },
      "command": [
        "netcoredbg",
        "--interpreter=vscode"
      ],
      "configuration": {
        "cwd": "${workspaceRoot}"
      },
      "name": "netcoredbg"
    }
  }
}
